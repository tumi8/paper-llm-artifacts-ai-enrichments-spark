#!/bin/bash
set -eux

# Add public SSH key to authorized_keys
PUBLIC_KEY="$(pos_get_variable public-key -r)"
mkdir -p /root/.ssh
echo "$PUBLIC_KEY" >> /root/.ssh/authorized_keys
chmod 600 /root/.ssh/authorized_keys
chmod 700 /root/.ssh
